<%- include('partials/header') %>

    <main>
        <h1>
            <%= title %>
        </h1>

        <ul class="item-list">
            <% users.forEach(user=> { %>
                <li>
                    <%= user.name %> — <%= user.email %> (<%= user.role_name %>)
                </li>
                <% }) %>
        </ul>
    </main>

<%- include('partials/footer') %>